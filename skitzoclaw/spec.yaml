schemaVersion: "1"
kind: agent
name: skitzoclaw
displayName: SkitzoClaw
description: Honkinwaffles SkitzoClaw - Keeping Claude in a sandbox

agent:
  image: "docker/sandbox-templates:claude-code-docker"
  aiFilename: CLAUDE.md
  persistence: persistent
  entrypoint:
    run: [claude, --permission-mode, plan]

network:
  serviceDomains:
    api.anthropic.com: anthropic
  serviceAuth:
    anthropic:
      headerName: x-api-key
      valueFormat: "%s"
  allowedDomains:
    - "downloads.claude.ai:443"
    - "claude.com:443"
    - "claude.ai:443"
    - "console.anthropic.com:443"

environment:
  variables:
    IS_SANDBOX: "1"
    ANTHROPIC_MODEL: "claude-haiku-4-5-20251001"

commands:
  install:
    - command: "apt-get update && apt-get install -y --no-install-recommends jq && rm -rf /var/lib/apt/lists/*"
      user: "0"
      description: Install jq
    - command: "chmod +x /usr/local/bin/gitignore-guard.sh"
      user: "0"
      description: Make gitignore guard hook executable
    - command: "chmod 0644 /etc/claude-code/managed-settings.json"
      user: "0"
      description: Set managed settings permissions
